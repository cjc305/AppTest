package com.apptest.feature.inbox.data

// SupabaseInboxRepository moved to :core:data (com.apptest.core.data.inbox.SupabaseInboxRepository).
// :feature:inbox should only depend on :core:domain (InboxRepository interface).
