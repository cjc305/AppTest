package com.apptest.feature.inbox.data.di

// InboxDataModule moved to :app module (com.apptest.app.di.InboxDataModule).
// :feature:inbox should only depend on :core:domain (InboxRepository interface),
// not on :core:data (SupabaseInboxRepository implementation).
